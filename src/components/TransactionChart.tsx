import React from 'react';\nimport { useTheme } from '../hooks/useTheme';\n\ninterface TransactionChartProps {\n  data: any[];\n}\n\nexport function TransactionChart({ data }: TransactionChartProps) {\n  const { theme } = useTheme();\n\n  // Simple mock chart - in a real app, you'd use Chart.js or similar\n  return (\n    <div className={`h-64 flex items-end justify-center space-x-1 p-4 rounded-lg ${\n      theme === 'dark' ? 'bg-gray-700/20' : 'bg-gray-50'\n    }`}>\n      {[40, 65, 25, 80, 55, 90, 45].map((height, index) => (\n        <div\n          key={index}\n          className={`w-8 rounded-t transition-all duration-500 hover:opacity-80 ${\n            theme === 'dark' ? 'bg-blue-400' : 'bg-blue-500'\n          }`}\n          style={{ height: `${height}%` }}\n        />\n      ))}\n    </div>\n  );\n}