import React from 'react';\nimport { useTheme } from '../hooks/useTheme';\n\ninterface AlertCardProps {\n  type: 'success' | 'warning' | 'error' | 'info';\n  title: string;\n  message: string;\n  action?: () => void;\n  actionText?: string;\n}\n\nexport function AlertCard({ type, title, message, action, actionText }: AlertCardProps) {\n  const { theme } = useTheme();\n  \n  const typeConfig = {\n    success: {\n      icon: '✅',\n      bgColor: theme === 'dark' ? 'bg-green-900/20' : 'bg-green-50',\n      borderColor: 'border-green-500',\n      textColor: theme === 'dark' ? 'text-green-300' : 'text-green-700',\n      buttonColor: theme === 'dark' ? 'bg-green-700 hover:bg-green-600' : 'bg-green-600 hover:bg-green-700',\n    },\n    warning: {\n      icon: '⚠️',\n      bgColor: theme === 'dark' ? 'bg-yellow-900/20' : 'bg-yellow-50',\n      borderColor: 'border-yellow-500',\n      textColor: theme === 'dark' ? 'text-yellow-300' : 'text-yellow-700',\n      buttonColor: theme === 'dark' ? 'bg-yellow-700 hover:bg-yellow-600' : 'bg-yellow-600 hover:bg-yellow-700',\n    },\n    error: {\n      icon: '❌',\n      bgColor: theme === 'dark' ? 'bg-red-900/20' : 'bg-red-50',\n      borderColor: 'border-red-500',\n      textColor: theme === 'dark' ? 'text-red-300' : 'text-red-700',\n      buttonColor: theme === 'dark' ? 'bg-red-700 hover:bg-red-600' : 'bg-red-600 hover:bg-red-700',\n    },\n    info: {\n      icon: 'ℹ️',\n      bgColor: theme === 'dark' ? 'bg-blue-900/20' : 'bg-blue-50',\n      borderColor: 'border-blue-500',\n      textColor: theme === 'dark' ? 'text-blue-300' : 'text-blue-700',\n      buttonColor: theme === 'dark' ? 'bg-blue-700 hover:bg-blue-600' : 'bg-blue-600 hover:bg-blue-700',\n    },\n  };\n\n  const config = typeConfig[type];\n\n  return (\n    <div className={`p-4 rounded-lg border-l-4 ${\n      config.bgColor\n    } ${\n      config.borderColor\n    } ${\n      config.textColor\n    }`}>\n      <div className=\"flex items-start justify-between\">\n        <div className=\"flex items-start space-x-3\">\n          <span className=\"text-xl flex-shrink-0\">{config.icon}</span>\n          <div className=\"flex-1\">\n            <h4 className=\"font-semibold text-sm mb-1\">{title}</h4>\n            <p className=\"text-sm opacity-90\">{message}</p>\n          </div>\n        </div>\n        \n        {action && actionText && (\n          <button\n            onClick={action}\n            className={`ml-4 px-3 py-1 rounded text-xs font-medium text-white transition-colors duration-200 ${\n              config.buttonColor\n            }`}\n          >\n            {actionText}\n          </button>\n        )}\n      </div>\n    </div>\n  );\n}