import React from 'react';\nimport { useTheme } from '../hooks/useTheme';\n\ninterface TopPerformersProps {\n  performers: Array<{\n    customer_id: string;\n    weekly_pnl: number;\n  }>;\n}\n\nexport function TopPerformers({ performers }: TopPerformersProps) {\n  const { theme } = useTheme();\n\n  if (!performers || performers.length === 0) {\n    return (\n      <div className=\"text-center py-8\">\n        <div className=\"text-4xl mb-4\">🏆</div>\n        <p className={theme === 'dark' ? 'text-gray-400' : 'text-gray-600'}>\n          No performance data available\n        </p>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"space-y-3\">\n      {performers.map((performer, index) => {\n        const isPositive = performer.weekly_pnl >= 0;\n        const medals = ['🥇', '🥈', '🥉', '🏅', '⭐'];\n        \n        return (\n          <div\n            key={performer.customer_id}\n            className={`flex items-center justify-between p-4 rounded-lg border transition-colors duration-200 ${\n              theme === 'dark' \n                ? 'bg-gray-700/30 border-gray-600' \n                : 'bg-gray-50 border-gray-200'\n            }`}\n          >\n            <div className=\"flex items-center space-x-3\">\n              <span className=\"text-2xl\">{medals[index] || '⭐'}</span>\n              <div>\n                <div className=\"font-semibold\">{performer.customer_id}</div>\n                <div className={`text-sm ${\n                  theme === 'dark' ? 'text-gray-400' : 'text-gray-600'\n                }`}>\n                  Rank #{index + 1}\n                </div>\n              </div>\n            </div>\n            \n            <div className=\"text-right\">\n              <div className={`font-bold text-lg ${\n                isPositive ? 'text-green-500' : 'text-red-500'\n              }`}>\n                {isPositive ? '+' : ''}${performer.weekly_pnl.toLocaleString()}\n              </div>\n              <div className={`text-xs ${\n                theme === 'dark' ? 'text-gray-400' : 'text-gray-600'\n              }`}>\n                Weekly P&L\n              </div>\n            </div>\n          </div>\n        );\n      })}\n    </div>\n  );\n}